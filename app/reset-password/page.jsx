'use client';
import { createBrowserClient } from '@supabase/ssr';
import { useState } from 'react';
export default function ResetPasswordPage(){const [message,setMessage]=useState('');async function submit(event){event.preventDefault();const password=new FormData(event.currentTarget).get('password');const client=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);const {error}=await client.auth.updateUser({password});if(error)return setMessage(error.message);setMessage('Password updated. You can now return to the CRM.');}return <main className="content" style={{maxWidth:460,margin:'8vh auto'}}><section className="panel"><div className="panelHeader"><div><p className="eyebrow">Account recovery</p><h1>Choose a new password</h1></div></div><div className="panelBody"><form className="grid" onSubmit={submit}><input name="password" type="password" minLength="10" required placeholder="New password (10+ characters)"/><button>Update password</button>{message?<p className="muted">{message}</p>:null}</form></div></section></main>}
