'use client';
import { createBrowserClient } from '@supabase/ssr';
import { useState } from 'react';
export default function ForgotPasswordPage(){const [message,setMessage]=useState('');async function submit(event){event.preventDefault();const email=new FormData(event.currentTarget).get('email');const client=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);const {error}=await client.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});setMessage(error?error.message:'If that account exists, a password-reset email has been sent.');}return <main className="content" style={{maxWidth:460,margin:'8vh auto'}}><section className="panel"><div className="panelHeader"><div><p className="eyebrow">Account recovery</p><h1>Reset password</h1></div></div><div className="panelBody"><form className="grid" onSubmit={submit}><input name="email" type="email" required placeholder="Email address"/><button>Send reset email</button>{message?<p className="muted">{message}</p>:null}</form></div></section></main>}
